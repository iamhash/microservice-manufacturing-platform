<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.bugstack.ai.qualityservice.mapper.QualityRecordMapper">
    <resultMap id="QualityRecordResult" type="cn.bugstack.ai.qualityservice.domain.QualityRecord">
        <id property="id" column="id"/>
        <result property="stage" column="stage"/>
        <result property="itemCode" column="item_code"/>
        <result property="result" column="result"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="selectQualityRecord">
        select id, stage, item_code, result, create_time from quality_record
    </sql>

    <insert id="insert" parameterType="cn.bugstack.ai.qualityservice.domain.QualityRecord">
        insert into quality_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="stage != null">stage,</if>
            <if test="itemCode != null">item_code,</if>
            <if test="result != null">result,</if>
            <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="stage != null">#{stage},</if>
            <if test="itemCode != null">#{itemCode},</if>
            <if test="result != null">#{result},</if>
            <if test="createTime != null">#{createTime},</if>
        </trim>
    </insert>

</mapper>