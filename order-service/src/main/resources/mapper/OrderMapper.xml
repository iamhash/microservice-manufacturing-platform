<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.bugstack.ai.orderservice.mapper.OrderMapper">

    <resultMap id="OrderInfoResult" type="cn.bugstack.ai.orderservice.domain.OrderInfo">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="modelCode" column="model_code"/>
        <result property="quantity" column="quantity"/>
        <result property="priority" column="priority"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="selectOrderInfo">
        select id, order_no, model_code, quantity, priority, status, create_time from order_info
    </sql>

    <insert id="insert" parameterType="cn.bugstack.ai.orderservice.domain.OrderInfo">
        insert into order_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="orderNo != null">order_no,</if>
            <if test="modelCode != null">model_code,</if>
            <if test="quantity != null">quantity,</if>
            <if test="priority != null">priority,</if>
            <if test="status != null">status,</if>
            <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="orderNo != null">#{orderNo},</if>
            <if test="modelCode != null">#{modelCode},</if>
            <if test="quantity != null">#{quantity},</if>
            <if test="priority != null">#{priority},</if>
            <if test="status != null">#{status},</if>
            <if test="createTime != null">#{createTime},</if>
        </trim>
    </insert>

    <select id="selectById" resultMap="OrderInfoResult">
        <include refid="selectOrderInfo"/>
        where id = #{id}
    </select>


</mapper>